- 配置文件采用 YAML 清单式声明并通过 `use: package.module:class` 反射路径动态加载实现类（models/tools/guardrails 等）。
- 敏感凭据一律通过 `$ENV_VAR` 占位符引用，禁止硬编码到 config.yaml。
- 新增配置字段需递增 `config_version` 并通过 `make config-upgrade` 合并变更。
- 跨子模块共享的契约与文档集中放在 docs_and_contracts，避免散落在各子模块 README 中。