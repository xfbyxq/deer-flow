- 计划与规格文件统一使用 `YYYY-MM-DD-<主题>.md` 命名，并按 plans/specs 分目录存放，便于按时间排序与双向对照。
- 跨语言共享的数据格式以 `contracts/*.json` 中的 `valid_status_values` 等字段作为单一事实来源，各语言实现直接消费该 fixture 而非各自硬编码枚举。
- 设计文档在涉及具体规则时显式指向对应 SKILL.md 或源码路径，并以“当文档与代码冲突时以代码为准”的声明确立可执行契约优先。