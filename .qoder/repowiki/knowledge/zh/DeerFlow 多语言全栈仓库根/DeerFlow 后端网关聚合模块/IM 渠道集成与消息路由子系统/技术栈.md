基于 Python asyncio 的轻量消息总线；通过 `langgraph_sdk` 与 Gateway 的 LangGraph-compatible API 通信（`runs.wait` 同步模式 + SSE `messages-tuple`/`values` 流式模式）；使用 `httpx.AsyncClient` 拉取外部平台附件；JSON 文件原子写入（tempfile + replace）作为 channel-thread 映射的持久化后端。