FastAPI + Pydantic v2（`Field(..., description=...)` 驱动 OpenAPI 文档）；SSE 通过 `StreamingResponse` 配合 `sse_consumer` 消费 `StreamBridge`；LangGraph Checkpointer (`InMemorySaver`/`AsyncSqliteSaver`/`AsyncPostgresSaver`) 作为线程状态与断点的唯一事实源；uuid6 用于时间有序 checkpoint id。