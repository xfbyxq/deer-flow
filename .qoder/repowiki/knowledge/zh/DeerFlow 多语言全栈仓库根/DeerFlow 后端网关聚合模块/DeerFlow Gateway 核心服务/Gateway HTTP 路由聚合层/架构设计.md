本目录是网关的 HTTP 路由集合，每个子文件对应一个独立业务域（核心对话、控制台、认证反馈、模型/技能/上传等），各自定义 `router = FastAPI()` 并通过 `include_router` 挂载到顶层 app；跨模块共享能力集中在 `app.gateway.pagination.trim_run_message_page` 等工具中，由所有路由复用以实现一致的游标分页语义。`routers/__init__.py` 仅显式导出部分模块供外部引用，其余路由仍通过 `gateway/app.py` 直接 import 并注册，形成“按域拆分 + 单点装配”的聚合模式。