基于 Starlette `BaseHTTPMiddleware` 与 FastAPI Request/Response 实现的 ASGI 中间件；JWT 解码与错误类型来自 `app.gateway.auth.jwt` 与 `app.gateway.auth.errors`；LangGraph 兼容层通过 `langgraph_sdk.Auth` 注册 `@authenticate`/`@on` 钩子。