- 所有公开导出均声明为纯函数或无副作用工具类，不持有 HTTP/FastAPI 框架依赖，保证可在任意上下文中 import。
- 对外部可选依赖（`pymupdf4llm`、`readabilipy` 子进程等）采用 try/except ImportError 或 try/except CalledProcessError 的懒加载模式，缺失时回退到降级路径而非抛出异常。
- 配置项读取统一通过 `_get_uploads_config_value` / `_default_*` 工厂函数完成，并对非法值记录 warning 后落回到安全默认值。
- 面向 LangChain 的消息/内容结构使用 `isinstance(content, str)` → `isinstance(content, list)` → `Mapping` 的多分支解析模式，对未知形状一律 `str(...)` 兜底。