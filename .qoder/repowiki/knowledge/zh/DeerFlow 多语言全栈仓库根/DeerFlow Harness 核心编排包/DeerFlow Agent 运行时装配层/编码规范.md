- Feature 开关统一采用 `bool | AgentMiddleware` 三态语义：False 禁用、True 使用内置默认实现、传入实例则替换自定义中间件。
- 中间件位置由 `@Next(Anchor)` / `@Prev(Anchor)` 装饰器声明 `_next_anchor` / `_prev_anchor` 属性，而非硬编码索引。
- 公共 API 通过 `__getattr__` 延迟导入，确保仅按需加载 lead_agent/middlewares/memory 等重型子包。