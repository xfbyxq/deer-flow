基于 langchain `AgentMiddleware` 抽象与 langgraph `Runtime`/`get_config` 获取运行时上下文；使用 `langchain_core.messages` 的 `SystemMessage`/`HumanMessage` 承载框架数据与用户数据，并通过 `additional_kwargs` 标记隐藏消息；使用 `asyncio.wait_for` + `asyncio.to_thread` 对可能阻塞的 tiktoken 下载做超时降级。