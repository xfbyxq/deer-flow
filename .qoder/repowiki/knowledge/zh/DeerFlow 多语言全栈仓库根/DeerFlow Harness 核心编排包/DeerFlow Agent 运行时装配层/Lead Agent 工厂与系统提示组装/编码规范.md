- 对可能循环引用的第三方模块（tools、middlewares、skills.describe 等）使用函数体内 `from ... import` 延迟导入，避免包初始化时的环依赖。
- 所有图内 `create_chat_model` 调用统一传入 `attach_tracing=False`，tracing callbacks 只在 `_make_lead_agent` 根节点通过 `config["callbacks"]` 追加一次。
- 中间件按严格顺序 append 到 list，并在注释中记录排序理由（如 ThreadDataMiddleware 必须在 SandboxMiddleware 之前、ClarificationMiddleware 始终最后）。
- 运行时开关通过 `RunnableConfig["configurable"]` / `context` 合并后的字典读取，并以 `cfg.get("xxx", default)` 形式提供默认值，保持向后兼容。
- 可复用的 prompt 片段以独立私有函数（`_build_*_section`）拆分并通过 `SYSTEM_PROMPT_TEMPLATE.format` 注入，使模板本身保持静态以便前缀缓存命中。