- 对第三方 tracing SDK（langchain、langfuse）采用函数体内局部 import，避免模块加载期的循环依赖。
- 对外接口在 Langfuse 未启用时返回空 dict / 空列表，让调用方可以无分支地合并或拼接结果。
- 通过 `get_enabled_tracing_providers()` + 显式 provider 名称字符串分派来扩展新的 tracing 后端，而非注册表模式。