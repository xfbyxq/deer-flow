- 每个领域子包采用 model.py（SQLAlchemy 映射）+ sql.py（异步仓库函数）的双文件拆分
- 所有模型继承自 base.py 提供的基类，统一使用 UUID 主键与审计时间戳字段
- 仓库函数统一通过 engine.py 导出的 AsyncSession 上下文访问数据库，不持有会话实例
- JSON 列查询一律经 json_compat.py 的方言适配包装，避免直接操作方言特定语法