PostgreSQL 后端需额外安装 `asyncpg`：`cd backend && uv sync --all-packages --extra postgres`；SQLite 部署建议开启 WAL 模式（由引擎连接事件自动设置 `PRAGMA journal_mode=WAL; synchronous=NORMAL; busy_timeout=30000; foreign_keys=ON`）。