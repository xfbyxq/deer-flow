SQLAlchemy 2.0 async ORM（`AsyncEngine`/`AsyncSession`/`async_sessionmaker`）+ Alembic 异步迁移；PostgreSQL 驱动 `asyncpg`（可选 extra），SQLite 使用原生 `sqlite3` 驱动并通过 PRAGMA 调优；Alembic 通过 `context.config.set_main_option` 从内存构造配置而非读取磁盘 `alembic.ini`。