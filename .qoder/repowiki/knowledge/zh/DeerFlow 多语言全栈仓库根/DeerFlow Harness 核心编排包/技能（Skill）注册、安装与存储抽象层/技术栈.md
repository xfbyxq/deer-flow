ZIP 归档安装（zipfile）、LLM 驱动的安全审查（通过 `deerflow.models.create_chat_model` 调用配置的 `skill_evolution.moderation_model_name`）、基于魔术字节头识别 ELF/PE/Mach-O 可执行二进制、使用 `concurrent.futures.ThreadPoolExecutor` 桥接 async 安装到同步调用上下文。