- 每个可插拔后端（checkpointer/store/stream_bridge/events_store/runs.store）都同时提供同步工厂与异步上下文管理器，命名遵循 make_* / *_context 模式
- 跨模块共享的运行时键名集中定义于独立文件（如 secret_context.py 中的 SECRETS_CONTEXT_KEY），禁止散落在调用方硬编码
- 对可选依赖（redis）采用按需 import，顶层 __init__.py 显式不 re-export 其符号以避免冷启动副作用