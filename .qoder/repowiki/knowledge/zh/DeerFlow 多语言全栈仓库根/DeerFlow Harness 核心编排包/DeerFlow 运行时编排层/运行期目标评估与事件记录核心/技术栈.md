基于 LangChain `BaseCallbackHandler` 与 LangGraph checkpointer 协议（`aget_tuple`/`aput`/`get_next_version`），使用 `langgraph.checkpoint.base.empty_checkpoint` 与 `uuid6` 构造根检查点；goal evaluator 通过 `deerflow.models.create_chat_model(thinking_enabled=False)` 创建轻量非思考模型。