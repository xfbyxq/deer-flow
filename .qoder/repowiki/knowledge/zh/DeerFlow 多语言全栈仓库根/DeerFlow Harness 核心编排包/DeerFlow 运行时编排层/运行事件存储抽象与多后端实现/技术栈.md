SQLAlchemy async (AsyncSession + async_sessionmaker) 驱动 DB 后端；PostgreSQL 使用 `pg_advisory_xact_lock(hashtext(thread_id))` 替代不支持的 `SELECT MAX(...) FOR UPDATE`；JSONL 后端通过 `asyncio.to_thread` 将阻塞 I/O 移出事件循环。