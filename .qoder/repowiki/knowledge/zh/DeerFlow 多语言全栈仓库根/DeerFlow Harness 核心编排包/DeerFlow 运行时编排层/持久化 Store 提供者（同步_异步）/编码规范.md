- 后端实现采用延迟 import（函数体内 from ... import）包裹，使缺失可选包时仅在使用该后端时才抛出 ImportError。
- 同步与异步工厂成对出现：_sync_store_cm/_async_store 负责构造与 teardown，外层 get_store/store_context/make_store 负责配置解析与生命周期封装。
- 配置解析统一走 _resolve_store_config，先检查 legacy checkpointer 段再回退到 unified database 段，保证 Store 与 Checkpointer 行为一致。
- SQLite 连接字符串一律经 resolve_sqlite_conn_str 规范化，并通过 ensure_sqlite_parent_dir 确保父目录存在，避免首次写入时的 FileNotFoundError。