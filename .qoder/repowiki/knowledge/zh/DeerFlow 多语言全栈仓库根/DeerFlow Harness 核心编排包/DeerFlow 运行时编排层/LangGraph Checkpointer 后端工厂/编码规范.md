- 通过 try/except ImportError 延迟 import 具体 saver 类并在缺失时抛出带安装指引的 ImportError，使可选后端在无对应包时给出明确修复命令。
- 对 sqlite 连接字符串统一走 `_sqlite_utils.resolve_sqlite_conn_str` + `ensure_sqlite_parent_dir` 预处理，确保文件路径存在后再交给 saver。
- 使用 `@contextlib.contextmanager` / `@contextlib.asynccontextmanager` 封装后端构造与清理，将 `saver.setup()` 放在 with 块内部，让上层仅关心 yield 出的实例。
- 同步单例采用「外层快速判空 + 持锁内层二次判空」的双重检查模式，配合独立的 `_checkpointer_ctx` 记录上下文以便 `reset_checkpointer` 正确关闭连接。