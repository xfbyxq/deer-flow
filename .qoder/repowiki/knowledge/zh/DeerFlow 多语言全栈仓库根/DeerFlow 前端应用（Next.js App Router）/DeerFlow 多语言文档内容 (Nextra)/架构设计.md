目录以 `en/`、`zh/` 两个顶级子目录分别承载英文与中文版本，结构完全镜像；每个文档目录内通过 `_meta.ts` 使用 Nextra 的 `MetaRecord` 声明侧边栏标题与路由类型（如 `type: "page"`），MDX 文件即页面源。顶层路由映射到 `index`、`introduction`、`harness`、`application`、`tutorials`、`reference`、`posts` 等板块，其中 `reference/model-providers/` 与 `posts/releases|weekly/` 采用嵌套子目录进一步细分。该模块仅产出纯文本内容，不引入任何业务逻辑，由上层 Nextra 站点工程消费。