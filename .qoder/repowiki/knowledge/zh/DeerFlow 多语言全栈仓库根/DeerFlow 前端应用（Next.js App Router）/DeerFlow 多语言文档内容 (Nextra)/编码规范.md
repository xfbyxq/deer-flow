- 每个文档目录都提供对应的 `_meta.ts`，通过导出 `MetaRecord` 显式声明该目录下所有页面的侧边栏标题与路由类型。
- 中英文版本保持一一对应的目录与文件名镜像，同一功能在 `en/` 与 `zh/` 下各有一份独立 MDX。
- MDX 文件命名采用 kebab-case（如 `quick-start.mdx`、`agents-and-threads.mdx`），并在 `_meta.ts` 中以相同字符串键引用。