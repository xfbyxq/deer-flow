- E2E 用例在 `test.beforeEach` 中调用 `mockLangGraphAPI(page, options?)` 注入内存后端，再通过 `page.goto('/workspace/...')` 导航到目标页面。
- 需要自定义流式响应的用例会先 `page.route('**/runs/stream', ...)` 覆盖默认 mock，再调用 `handleRunStream(route)` 发送 SSE 片段。
- 单元测试使用 `rs.mock('@/core/xxx', factory)` 替换模块依赖，并用 `await import('@/core/xxx')` 延迟加载被测函数，确保 mock 在模块初始化前生效。
- 断言统一使用 `expect(...).toBeVisible({ timeout: 15_000 })` 等 Playwright 内置匹配器，避免手写轮询等待。
- 跨用例复用的确定性 ID 集中在 `mock-api.ts` 顶部常量（`MOCK_THREAD_ID`、`MOCK_RUN_ID` 等），避免在 spec 中硬编码字符串。