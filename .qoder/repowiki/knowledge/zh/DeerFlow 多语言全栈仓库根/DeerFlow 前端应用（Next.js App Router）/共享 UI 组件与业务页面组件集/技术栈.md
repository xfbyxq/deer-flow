React + Next.js 客户端组件模式；UI 基座采用 Radix UI（slot、dialog、dropdown-menu、tooltip、tabs、switch、select、command 等）+ class-variance-authority 变体系统；Markdown 渲染基于 streamdown（含 marked tokenizer 崩溃保护）；主题切换使用 next-themes；侧边栏交互来自自定义 `components/ui/sidebar`。