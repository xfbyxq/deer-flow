- 每个子目录以 `index.ts` 作为唯一对外入口，内部实现文件按需命名并按职责拆分（如 blog 将类型、解析器、数据聚合函数分别放在同文件的不同作用域）。
- 对外只暴露最小 API：Hook 返回结构化对象而非直接调用原生 API，类型包只 export interface 不附带实现。
- 对不可信输入采用防御式解析：`parseTags`、`parseDate`、`matchTags` 等函数显式校验类型后再降级到空值。