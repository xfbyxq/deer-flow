- 每个子模块只导出纯函数或常量，不维护内部可变状态，便于在 React 渲染路径中安全调用。
- 对外 API 采用命名导出而非默认导出，方便 Tree-shaking 和 IDE 自动补全。
- 需要国际化文案时统一通过注入的 `Translations` 对象访问（如 `t.toolCalls.searchFor(...)`），避免直接硬编码字符串。
- 对不可信输入一律先 try/catch 再返回 undefined/null 的降级值（如 `tryParseJSON`、`normalizeUrl`）。