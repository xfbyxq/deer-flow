生产部署统一通过 `scripts/deploy.sh [build|start|down]` 启动，它会自动从 `.env` 读取变量、生成 `BETTER_AUTH_SECRET` 与 `DEER_FLOW_INTERNAL_AUTH_TOKEN`、根据 `config.yaml` 的 `sandbox.use` 字段决定是否需要叠加 `docker-compose.dood.yaml`（AioSandboxProvider）或拉起 provisioner 服务；交互式初始化则运行 `uv run python scripts/setup_wizard.py`。