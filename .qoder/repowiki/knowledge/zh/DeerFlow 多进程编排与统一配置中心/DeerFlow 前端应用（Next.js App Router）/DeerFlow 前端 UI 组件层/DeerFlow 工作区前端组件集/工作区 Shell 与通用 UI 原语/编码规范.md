- 所有组件文件顶部标注 "use client"，确保可在客户端渲染并与浏览器 API（如 usePathname、useTheme）交互。
- 样式统一通过 cn() 合并 Tailwind 类名，避免硬编码 className 拼接。
- 文本全部经 useI18n().t 取键值，禁止直接写死中英文字符串。
- 子领域目录采用 index.ts 做 barrel 聚合导出，外部通过单一路径引入该领域的全部导出。
- 异步副作用使用 AbortController 包装（如 goal 命令），并通过 isCurrentGoalRequest 守卫竞态，防止旧请求覆盖新状态。